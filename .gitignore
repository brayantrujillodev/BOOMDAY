*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
desktop.ini
/build
**/build/
/captures
.externalNativeBuild
.cxx
local.properties
.kotlin/

# Firebase config y credenciales — no son secretos críticos (el API key de
# google-services.json está restringido por SHA-1/paquete, no da acceso de
# servidor), pero no se versionan: cada desarrollador/CI usa el propio.
# Ver README.md → "Setup para desarrolladores" para cómo obtenerlo.
app/google-services.json
*.jks
*.keystore
key.properties

# Firebase CLI local artifacts
firebase-debug.log
firestore-debug.log
ui-debug.log
.firebase/